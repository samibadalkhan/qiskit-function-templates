name: Circuit Function - Executor Tests

on:
  push:
    branches: [main]
    paths:
      - 'circuit_functions/executor/**'
      - 'requirements-dev.txt'
      - 'requirements-e2e.txt'
      - 'tox.ini'

  pull_request:
    branches: [main]
    paths:
      - 'circuit_functions/executor/**'
      - 'requirements-dev.txt'
      - 'requirements-e2e.txt'
      - 'tox.ini'

jobs:
  call-tests:
    uses: ./.github/workflows/_unit-tests.yml
    with:
      dir: circuit_functions/executor
      os-list: '["macOS-latest", "ubuntu-latest", "windows-latest"]'
