qiskit>=2.0.0
qiskit-ibm-runtime>=0.34.0
qiskit-serverless>=0.20.0
numpy>=1.26.0
pydantic>=2.0.0

# Test-only: the unit tests build seeded AerSimulator reference values.
# _unit-tests.yml installs this file, so the dependency must live here.
qiskit-aer>=0.15
